block variables

doctype html
html(lang=ru)
  head
    title= pageTitle
    meta(name="description", content=description)
  body(class= pageClass)
    .wrapper
      #app
        .content-wrapper
          block content
          